#include "main.h"

/**
 * _sqrt_recursion - returns the natural square root of a number
 * @n: the number to find the square root of
 *
 * Return: -1 (no helper available, constraint impossible)
 */
int _sqrt_recursion(int n)
{
	if (n < 0)
		return (-1);

	return (-1);
}
